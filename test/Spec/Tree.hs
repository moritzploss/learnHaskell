module Spec.Tree where

import Test.HUnit
import qualified Lib

--testCreateTree :: Test
--testCreateTree =
--  TestCase $ assertEqual
--    "Create a tree with 3 elements"
--    (Node a (Tree a) (Tree a))
--    (Lib.treeFromList [1, 2, 3])

testList :: Test
testList = TestList [
--     testCreateTree
   ]
